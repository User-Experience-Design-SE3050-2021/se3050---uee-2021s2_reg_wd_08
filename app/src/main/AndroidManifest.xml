<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.cargillsshop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cargillsshop"
        tools:replace="android:theme">
        <activity
            android:name=".m_OTP_Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".m_cartlist_Activity"
            android:exported="true">
        </activity>
        <activity
            android:name=".m_paymentoption"
            android:exported="true"></activity>
        <activity
            android:name=".mpayment_form"
            android:exported="true" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".CategoryActivity2" />
        <activity android:name=".cartActivity" />
        <activity android:name=".HomeActivity2" />
        <activity android:name=".CategorycardView" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>